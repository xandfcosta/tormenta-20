# yaml-language-server: $schema=https://raw.githubusercontent.com/LucasPickering/slumber/refs/tags/v4.2.0/schemas/collection.json
# ^ This enables schema validation and autocomplete in your editor
# https://github.com/redhat-developer/yaml-language-server

# For basic usage info, see:
# https://slumber.lucaspickering.me/getting_started.html
# For all collection options, see:
# https://slumber.lucaspickering.me/api/request_collection/index.html

name: RPG

# Profiles are groups of data you can easily switch between. A common usage is
# to define profiles for various environments of a REST service

profiles:
  local:
    name: Local
    data:
      host: http://localhost:3000

requests:
  devices:
    name: Characters
    requests:
      complete_all:
        name: All Complete
        method: GET
        url: "{{ host }}/characters"
      overview_all:
        name: All Overviews
        method: GET
        url: "{{ host }}/characters/overview"
      complete:
        name: One Complete
        method: GET
        url: "{{ host }}/characters/1"
      overview:
        name: One Overview
        method: GET
        url: "{{ host }}/characters/1/overview"
      expertisies:
        name: One Expertisies
        method: GET
        url: "{{ host }}/characters/1/expertisies"
      inventoru:
        name: One Inventory
        method: GET
        url: "{{ host }}/characters/1/inventory"
      abilities:
        name: One Abilties
        method: GET
        url: "{{ host }}/characters/1/abilities"
      create:
        name: Create character
        method: POST
        url: "{{ host }}/characters"
        body:
          type: json
          data:
            {
              "name": "Kharvos",
              "player": "Xand",
              "origin": "Refugiado",
              "races": ["Minotauro", "Lefou"],
              "classes": ["Bárbaro"],
              "divinity": "Tauron",
              "description": "",
            }
